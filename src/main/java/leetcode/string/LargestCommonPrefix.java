package leetcode.string;

// https://leetcode.com/problems/longest-common-prefix/
public class LargestCommonPrefix {
    public String longestCommonPrefix(String[] strs) {
        // start from the first index of both the array and string
        // check if its in the other index, if its exists remove it from each index
        // if it reaches through the entire array and its found it all of them add it to the string
        return "";
    }
}
